[package]
name = "reed-solomon"
version = "0.1.0"
edition = "2021"

[dependencies]

[lib]
doctest = false

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "benchmarks"
harness = false

[profile.bench]
debug = 1
